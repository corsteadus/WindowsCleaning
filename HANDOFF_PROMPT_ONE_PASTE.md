# One-Paste Sync Prompt (copy everything below the line into the duplicate app's agent)

---

You are updating this app to match a stabilized source copy. Work through the phases below IN ORDER. Before each phase, check whether it is already implemented — if so, skip it and say so. After each phase, run its verification and do not continue until green. Commit after each completed phase.

GLOBAL RULES (apply to every phase):
- No database schema changes, no secrets changes, no publishing/deploying.
- Live checks against the running app must be NON-MUTATING (read-only GETs, or validation/404/400 checks). Never convert/create/edit/delete real records — successful-path behavior is proven by executable tests only.
- All typechecks must end at 0 errors: `pnpm --filter @workspace/api-server run typecheck`, `pnpm --filter @workspace/crm run typecheck`, `pnpm run typecheck:libs`.
- No `any`, no ts-ignore, no `as unknown as`, no unsafe casts. Use runtime guards or proper generics. For Drizzle transactions type: `type DrizzleTX = Parameters<Parameters<typeof db.transaction>[0]>[0];`
- Tests run with `node --test --experimental-strip-types` via each package's `test` script; test files import with the `.ts` extension (`.js` fails here).

PHASE 0 — ORIENT (no changes): Run all three typechecks and any existing test scripts; check which of these files exist: artifacts/api-server/src/lib/{date.ts,quote-convert.ts,lead-convert.ts}, artifacts/crm/src/lib/{recurring-plan-form.ts,job-date.ts,customer-list.ts,property.ts}; check whether lib/api-spec/openapi.yaml uses propertyName or propertyNickname; check whether POST /leads/:id/convert uses db.transaction + pg_advisory_xact_lock or a bare insert. Report which phases below are needed, then proceed.

PHASE 1 — RADIX SELECT SENTINEL FIX: Create artifacts/crm/src/lib/recurring-plan-form.ts exporting CUSTOMER_NONE / PROPERTY_NONE / SERVICE_NONE = "none" plus pure helpers converting between sentinel select values and `number | undefined` API payload ids. Refactor pages/RecurringPlanNew.tsx to use them for customer/property/service selects — never pass "" as a Radix <Select> value; unset selections submit undefined. Add recurring-plan-form.test.ts (node:test) covering sentinel and id round-trips and unset payload conversion; add it to the crm test script. Verify: crm tests + typecheck green.

PHASE 2 — BLOCK COMPLETING FUTURE-DATED JOBS: API — create src/lib/date.ts exporting businessDateStr(now = new Date()) returning YYYY-MM-DD via toLocaleDateString("en-CA"), and isScheduledInFuture(scheduledDate, today = businessDateStr()) using pure string comparison (null/undefined → false). In routes/jobs.ts status updates, reject transitioning to "completed" when the job's scheduledDate is in the future → 400 with a clear message. CRM — create src/lib/job-date.ts (todayDateStr, isJobScheduledInFuture, same semantics); disable the Mark Completed action in pages/JobDetail.tsx for future-dated jobs with an explanatory hint. Tests: date.test.ts (api) + job-date.test.ts (crm) covering today/past/future/null/month-year boundaries; add to test scripts. Verify: both suites + typechecks green.

PHASE 3 — ZERO TYPESCRIPT ERRORS: Bring api-server, crm, and libs typechecks to 0 errors under the global safety rules. If list endpoints return unknown-shaped data, create crm src/lib/customer-list.ts exporting `extractCustomerArray<T extends object>(raw: unknown): T[]` — runtime-guarded, accepts a bare array or a `{ data: [...] }` envelope, returns [] otherwise — and use it in AdminImport/JobNew/QuoteNew instead of casting. Add customer-list.test.ts (bare array, envelope, empty, null, garbage). Verify: all typechecks 0, tests green.

PHASE 4 — RENAME propertyNickname → propertyName END-TO-END: Rename the field in lib/api-spec/openapi.yaml on job/quote/recurringPlan schemas; regenerate lib/api-zod and lib/api-client-react with the repo's codegen. Update the property-enrichment mapping in routes/jobs.ts, routes/quotes.ts, routes/recurring_plans.ts to emit propertyName. CRM: in src/lib/property.ts add propertyLabel(p) (nickname/name → address fallback) and a job-list label helper; use them in Dashboard, Jobs, Quotes, Schedule, RecurringPlans, RecurringPlanDetail. Tests: property.test.ts (crm) + property-enrich.test.ts (api) covering fallback order and missing fields. NOTE: DB column names do not change. Verify: all typechecks 0, tests green, read-only curl of one job shows propertyName.

PHASE 5 — IDEMPOTENT QUOTE→JOB CONVERSION + POST /jobs GUARD: Create artifacts/api-server/src/lib/quote-convert.ts (pure module, NO drizzle/pg imports) exporting:
- quoteAdvisoryLockKey(quoteId) = raw quoteId (single-int lock namespace shared by BOTH endpoints below);
- buildJobLineItemsJson(lineItems) → JSON string of [{description, quantity, unitPrice, totalPrice}] with numbers (empty → null);
- interface ConvertAdapter { acquireAdvisoryLock; findJobByQuoteId; findQuoteById; findLeadById; fetchLineItems; setQuoteApproved; insertJob } — acquireAdvisoryLock is ALWAYS called first; a throw aborts with zero mutations;
- convertQuoteCore(quoteId, adapter) step order: lock → existing job check (→ kind "existing") → load quote (→ "notFound") → resolve customerId (customer-owned direct; lead-owned via lead.convertedCustomerId; unconverted lead → kind "error" telling the user to convert the lead first) → fetchLineItems → setQuoteApproved → insertJob (status "scheduled", jobNumber `J-${Date.now()}`); returns discriminated union existing | created(+forLog) | notFound | error;
- interface CreateQuotedJobAdapter + createQuotedJobCore(quoteId, jobValues, adapter): lock → existing job → "conflict" → missing quote → "quoteNotFound" → insertJob.
Routes: POST /quotes/:id/convert runs convertQuoteCore in db.transaction with a DrizzleTxAdapter whose acquireAdvisoryLock executes `SELECT pg_advisory_xact_lock(${quoteAdvisoryLockKey(quoteId)})`; "existing" → 200 and NO new activity log; "created" → write the activity log AFTER commit. POST /jobs with a quoteId runs createQuotedJobCore in db.transaction with the SAME lock key; duplicate → 409 (include existing job id), missing quote → 404; jobs without quoteId unchanged.
Tests (quote-convert.test.ts + job-create-quoted.test.ts, in-memory fake adapters; an AsyncMutex class — Promise-chain withLock(fn) that always advances even on rejection — simulates the pg lock): first conversion, repeat → existing with no second insert/approve, simultaneous same-quote → exactly one job, different quotes parallel, lock-first ordering, lock failure → no mutation, line items preserved as numbers / empty → null, lead-owned resolution, unconverted/missing lead errors, POST /jobs conflict + notFound. Add both to the api test script. Verify: api tests + typecheck green; live check limited to POST convert on a nonexistent quote id → 404.

PHASE 6 — IDEMPOTENT LEAD→CUSTOMER CONVERSION + CRM QUOTE-FLOW LINKS: Create artifacts/api-server/src/lib/lead-convert.ts (pure, NO drizzle/pg imports) exporting:
- LEAD_LOCK_CLASSID = 2 and leadAdvisoryLockKey(leadId) → readonly [LEAD_LOCK_CLASSID, leadId], used with the TWO-argument form pg_advisory_xact_lock(classid, objid). PostgreSQL keeps the (int4,int4) keyspace fully separate from the single-int quote locks, so collision is impossible. Do NOT use an additive offset.
- interface ConvertLeadAdapter<C> { acquireAdvisoryLock; findLeadById; findCustomerById; insertCustomer; markLeadConverted } — generic over the customer row type (no route casts). markLeadConverted MUST throw when zero rows were updated (lead deleted concurrently) so the shared transaction rolls back the customer insert.
- convertLeadCore(leadId, customerDate, adapter) step order: lock FIRST → findLeadById (→ "notFound") → if convertedCustomerId set: load that customer → kind "existing" (customer row missing → kind "staleLink" with the dangling id) → insertCustomer copying firstName, lastName, email, phone, address→billingAddress, city→billingCity, state→billingState, zip→billingZip, source, notes, clientType ?? "residential", status "active", customerDate = passed-in value → markLeadConverted(leadId, customer.id) setting status "won" + convertedCustomerId atomically. Lead row is preserved; no properties/contacts created. Returns existing | staleLink | notFound | created(+forLog {leadId, customerId}).
Route POST /leads/:id/convert (routes/leads.ts): NaN id → 400; run convertLeadCore inside db.transaction with DrizzleTxLeadAdapter (markLeadConverted uses .returning({id}) and throws on empty result); customerDate from businessDateStr(). Outcomes: notFound → 404; staleLink → 409; existing → 200 with the serialized customer and NO new activity log; created → exactly ONE activity log (entityType "lead", action "converted", toValue = String(customer.id)) written AFTER commit → 200.
Tests (lead-convert.test.ts, fake adapter + AsyncMutex): lock key is a pair with the classid constant and structurally distinct from scalar quote keys; first conversion copies fields, sets customerDate, marks lead won; repeat → existing, insertCount stays 1; staleLink; notFound; lock failure → call log contains only the lock call; insert failure → markLeadConverted never called; lead deleted mid-conversion → markLeadConverted throws, nothing marked; simultaneous same-lead → exactly one customer; different leads parallel; queued caller succeeds after a failed predecessor. Add to the api test script.
CRM: (a) LeadDetail.tsx — convert mutation onSuccess navigates to `/customers/${customer.id}` from the response; when lead.convertedCustomerId is set render a visible "View Customer Account" link to that customer (chevron icon aria-hidden="true"); Convert button hidden when status is "won" or "lost". (b) CustomerDetail.tsx — next to Schedule Job (shown when !isEditing && status === "active"), add a "New Quote" button navigating to `/quotes/new?customerId=${customer.id}`. (c) QuoteNew.tsx — read customerId from window.location.search with new URLSearchParams (wouter has no search-params hook), validate positive integer, fetch via the generated useGetCustomer hook (enabled only when valid), initialize the customer combobox selection ONCE (later manual selection wins); the submit payload derives customerId only from the selected-customer state.
Verify: api tests + both typechecks green; restart the API workflow; live checks limited to POST /api/leads/99999999/convert → 404 and POST /api/leads/abc/convert → 400.

PHASE 7 — FINAL VERIFICATION: Run api tests, crm tests, all three typechecks; restart API + CRM workflows and confirm clean startup; read-only spot checks (GET /api/leads?limit=1 → 200; the 404/400 convert validation checks above). Expected end state: api-server 112 tests passing across date/property-enrich/quote-convert/job-create-quoted/lead-convert; crm 4 suites passing (property, recurring-plan-form, job-date, customer-list); 0 type errors everywhere; quote/job locks on pg_advisory_xact_lock(quoteId), lead locks on pg_advisory_xact_lock(2, leadId); both convert endpoints idempotent — repeat calls return the existing entity with 200 and only one activity log ever. Report a results table.
